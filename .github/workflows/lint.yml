# This workflow is temporarily disabled for the project structure PR
# The full workflow configuration is backed up in _lint.yml.bak
# It will be restored in the feature/linting-only PR

name: Lint (Disabled)

on:
  # Workflows will only run when manually triggered
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is temporarily disabled"
